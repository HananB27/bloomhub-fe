// Re-export the global TechBadge. The catalogue lives in `src/lib/tech/`
// so it can be reused by any module (employees, projects, profiles, etc.).
export { TechBadge } from "@/components/tech/TechBadge";
